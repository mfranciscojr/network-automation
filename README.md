# network-automation

Work in progress

Ansible
- This will automate basic device config template such as NTP, Domain Name etc etc.

CCIE LAB 

Pre-requisite
sudo apt update && sudo apt upgrade
sudo apt install git python3-pip
sudo pip3 install --upgrade pip
pip3 install ansible paramiko
sudo mkdir /home/ansible
cd /home/ansible
git clone git@github.com:mfranciscojr/network-automation.git
cd /home/ansible/network-automation

